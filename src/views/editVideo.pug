extends layouts/main.pug

block content
         .form-container
                form(action=routes.editVideo(video.id), method="post")
                        input(type="text", name="title", placeholder="Title", required=true, value=video.title)
                        textarea(name="description", placeholder="Description", required=true)=video.description
                        input(type="submit", value="Join")
                a.form__container__link(href=routes.deleteVideo(video.id))
                        button.delete Delete Video